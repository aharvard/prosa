// import { theme } from '../../stitches.config'

// {Object.entries(theme.colors).map(([name, token]) => (
//   <div>
//     <div style={{ background: token.value, padding: '1rem' }}>
//       <p>{name}</p>
//     </div>
//   </div>
// ))}
